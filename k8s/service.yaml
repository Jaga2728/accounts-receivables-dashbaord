apiVersion: v1
kind: Service
metadata:
  name: accounts-receivables-service
  namespace: production
spec:
  type: LoadBalancer
  selector:
    app: accounts-receivables-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP